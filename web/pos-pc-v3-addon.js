(()=>{
const TEST_TABLE_API=U+'/functions/v1/uber-table-service-pc-test-api';
const TEST_QR='https://bpnkouymdvcogeaqjmxl.supabase.co/functions/v1/dexters-table-order-pc-test';
const TEST_SUNDAY_API=U+'/functions/v1/sunday-roast-pc-pos-test-api';
const SALES_KEY='dexters_pc_pos_sales_v3';
const CREDIT_TEST_KEY='dexters_pc_credit_payments_v3';
const $x=id=>document.getElementById(id);
const cashMoney=n=>'£'+Number(n||0).toFixed(2);
const readSales=()=>{try{return JSON.parse(localStorage.getItem(SALES_KEY)||'[]')}catch{return[]}};
const saveSales=a=>localStorage.setItem(SALES_KEY,JSON.stringify(a.slice(0,2000)));
const token=()=>S?.session?.access_token||'';
function topButton(id,text,before='staffBtn'){let b=$x(id);if(b)return b;const top=document.querySelector('.top');if(!top)return null;b=document.createElement('button');b.id=id;b.textContent=text;top.insertBefore(b,$x(before)||null);return b}
function modal(title,html){const d=document.createElement('div');d.className='modal';d.innerHTML='<div class="box"><h2>'+title+'</h2><div class="pcBody">'+html+'</div><div class="actions"><button class="cancel pcClose">Close</button></div></div>';document.body.appendChild(d);d.querySelector('.pcClose').onclick=()=>d.remove();return d}
function cartTotal(){return Number(String($x('total')?.textContent||'0').replace(/[^0-9.]/g,''))||0}
function cartSnapshot(){return (S.cart||[]).map(x=>({name:x.name,qty:Number(x.qty)||1,unit:Number(x.unit)||0,mods:x.mods||x.modifiers||[]}))}
function clearAfterPayment(){try{S.cart=[];if(typeof renderCart==='function')renderCart()}catch{}}
async function kitchenSend(){const b=$x('sendBtn');if(!b)return;try{b.click()}catch{}}
function recordSale(method,tendered=0,change=0,kind='sale',extra={}){const sale={id:'PC-'+Date.now()+'-'+Math.random().toString(16).slice(2),created_at:new Date().toISOString(),method,total:cartTotal(),tendered,change,kind,items:cartSnapshot(),mode:S.mode,tableNo:S.tableNo,staff:S.staffEmail||'',...extra};const a=readSales();a.unshift(sale);saveSales(a);window.dispatchEvent(new CustomEvent('dexters-pc-test-sale-complete',{detail:sale}));return sale}
function cashPay(){const total=cartTotal();if(total<=0)return alert('Add items first.');const v=prompt('Total '+cashMoney(total)+'\nCash received:',total.toFixed(2));if(v===null)return;const tender=Number(String(v).replace(/[^0-9.]/g,''));if(!Number.isFinite(tender)||tender<total)return alert('Cash received is less than the total.');const change=Math.round((tender-total)*100)/100;if(!confirm('Cash '+cashMoney(tender)+'\nChange due '+cashMoney(change)+'\nComplete sale?'))return;recordSale('cash',tender,change);kitchenSend();alert('CHANGE DUE: '+cashMoney(change))}
function cardPay(){const total=cartTotal();if(total<=0)return alert('Add items first.');if(!confirm('CARD '+cashMoney(total)+'\nPC TEST: simulate approved card payment?'))return;recordSale('card',total,0);kitchenSend();alert('Card payment recorded in PC TEST.')}
function installPay(){const pay=document.querySelector('.pay');if(!pay)return;if(!$x('cashBtn')){const c=document.createElement('button');c.id='cashBtn';c.className='cash';c.textContent='CASH';c.onclick=cashPay;pay.insertBefore(c,$x('sendBtn'))}if(!$x('cardBtn')){const c=document.createElement('button');c.id='cardBtn';c.className='cardbtn';c.textContent='CARD';c.onclick=cardPay;pay.insertBefore(c,$x('sendBtn'))}}
function salesView(){const rows=readSales();const html=rows.length?rows.slice(0,100).map(r=>'<div class="held"><b>'+new Date(r.created_at).toLocaleString()+'</b> · '+String(r.kind||'sale').toUpperCase()+' · '+String(r.method||'')+' · '+cashMoney(r.total||r.amount||0)+'</div>').join(''):'<p>No PC test sales yet.</p>';modal('Sales · PC TEST',html)}
function cashup(){const rows=readSales();let cash=0,card=0,credit=0;for(const r of rows){if(r.kind==='money_owed')credit+=Number(r.amount||0);if(r.method==='cash')cash+=Number(r.total||r.amount||0);if(r.method==='card')card+=Number(r.total||r.amount||0)}modal('Cash up · PC TEST','<div class="group"><div class="sumrow"><span>Cash</span><b>'+cashMoney(cash)+'</b></div><div class="sumrow"><span>Card</span><b>'+cashMoney(card)+'</b></div><div class="sumrow"><span>Money Owed payments</span><b>'+cashMoney(credit)+'</b></div><div class="sumrow grand"><span>Total</span><b>'+cashMoney(cash+card)+'</b></div></div><p style="color:#9eb0c5">PC test data only.</p>')}
async function creditSearch(q){const t=token();if(!t)throw Error('Sign in first.');const r=await fetch(U+'/rest/v1/rpc/credit_search_customers',{method:'POST',headers:{apikey:K,Authorization:'Bearer '+t,'Content-Type':'application/json'},body:JSON.stringify({p_query:q,p_limit:25})});const d=await r.json();if(!r.ok)throw Error(d.message||d.error||'Search failed');return Array.isArray(d)?d:(d.customers||d.results||[])}
function creditPayTest(acct,name,balance){const raw=prompt(name+'\nBalance '+cashMoney(balance/100)+'\nAmount paying in store:',(balance/100).toFixed(2));if(raw===null)return;const amount=Number(String(raw).replace(/[^0-9.]/g,''));if(!amount||amount<=0||amount>balance/100)return alert('Enter a valid amount up to the balance.');const method=(prompt('Payment method: type CASH or CARD','CASH')||'').trim().toLowerCase();if(!['cash','card'].includes(method))return alert('Choose CASH or CARD.');let tender=amount,change=0;if(method==='cash'){const x=Number(String(prompt('Cash received:',amount.toFixed(2))||'').replace(/[^0-9.]/g,''));if(!x||x<amount)return alert('Cash received is too low.');tender=x;change=Math.round((x-amount)*100)/100}const rows=readSales();rows.unshift({id:'CREDIT-'+Date.now(),created_at:new Date().toISOString(),kind:'money_owed',method,amount,total:amount,tendered:tender,change,account_id:acct,customer_name:name,mode:'TEST_ONLY_NO_LIVE_BALANCE_CHANGE'});saveSales(rows);let c=[];try{c=JSON.parse(localStorage.getItem(CREDIT_TEST_KEY)||'[]')}catch{}c.unshift(rows[0]);localStorage.setItem(CREDIT_TEST_KEY,JSON.stringify(c.slice(0,500)));alert('PC TEST payment recorded.\n'+(change?'Change due '+cashMoney(change)+'\n':'')+'Live Money Owed balance has NOT been changed yet.')}
function moneyOwed(){const d=modal('Money Owed · Pay in Store','<input id="pcCreditQ" class="field" placeholder="Customer name, phone or email"><button id="pcCreditSearch" class="confirm" style="border:0;border-radius:10px;padding:10px 14px;font-weight:900">Search</button><div id="pcCreditResults" style="margin-top:10px"></div><p style="color:#9eb0c5">Payments are simulated in this PC test. Live balances remain unchanged until final approval.</p>');d.querySelector('#pcCreditSearch').onclick=async()=>{const out=d.querySelector('#pcCreditResults');out.textContent='Searching…';try{const rows=await creditSearch(d.querySelector('#pcCreditQ').value);out.innerHTML=rows.length?rows.map((x,i)=>{const id=x.id||x.account_id;const name=x.customer_name||x.full_name||x.name||'Customer';const bp=Number(x.balance_pence||x.balance||0);return '<div class="held"><b>'+name+'</b><br>Balance: <b>'+cashMoney(bp/100)+'</b><br><button class="pcCreditPay" data-i="'+i+'">Take payment</button></div>'}).join(''):'No customers found.';out.querySelectorAll('.pcCreditPay').forEach(b=>b.onclick=()=>{const x=rows[Number(b.dataset.i)];creditPayTest(x.id||x.account_id,x.customer_name||x.full_name||x.name||'Customer',Number(x.balance_pence||x.balance||0))})}catch(e){out.textContent=e.message}}}
function qrCodes(){const labels={1:'Inside Table 1',2:'Inside Table 2',3:'Outside Table 1',4:'Outside Table 2',5:'Bar Chair 1',6:'Bar Chair 2',7:'Bar Chair 3'};const d=modal('Table QR Codes · PC TEST','<div id="pcQrs"></div>');const root=d.querySelector('#pcQrs');Object.entries(labels).forEach(([n,label])=>{const url=TEST_QR+'?table='+n;const wrap=document.createElement('div');wrap.className='held';wrap.innerHTML='<b>'+label+'</b><br><canvas width="150" height="150"></canvas><br><input class="field" readonly value="'+url+'">';root.appendChild(wrap);if(window.QRCode)QRCode.toCanvas(wrap.querySelector('canvas'),url,{width:150},()=>{})})}
async function sunday(){const d=modal('Sunday Roast · PC TEST','<div class="srGrid"><input id="sName" class="field" placeholder="Customer name"><input id="sPhone" class="field" placeholder="Phone"><input id="sDate" class="field" type="date"><input id="sSlot" class="field" placeholder="Collection time e.g. 13:30"><input id="sAdults" class="field" type="number" min="0" value="1" placeholder="Adult mixed"><input id="sKids" class="field" type="number" min="0" value="0" placeholder="Kids mixed"><select id="sPay" class="field"><option value="unpaid">Unpaid</option><option value="deposit_paid">Deposit / part paid</option><option value="paid_full">Paid in full</option></select><input id="sPaid" class="field" inputmode="decimal" value="0.00" placeholder="Amount paid"></div><button id="sCreate" class="confirm" style="border:0;border-radius:10px;padding:11px 14px;font-weight:900">Create test roast order</button><div id="sMsg"></div>');d.querySelector('#sCreate').onclick=async()=>{const b={action:'create',customer_name:d.querySelector('#sName').value,customer_phone:d.querySelector('#sPhone').value,collection_date:d.querySelector('#sDate').value,collection_slot:d.querySelector('#sSlot').value,meals:{mixed:Number(d.querySelector('#sAdults').value)||0,kids_mixed:Number(d.querySelector('#sKids').value)||0},payment_status:d.querySelector('#sPay').value,amount_paid_pence:Math.round((Number(d.querySelector('#sPaid').value)||0)*100)};const r=await fetch(TEST_SUNDAY_API,{method:'POST',headers:{apikey:K,Authorization:'Bearer '+token(),'Content-Type':'application/json'},body:JSON.stringify(b)});const x=await r.json();d.querySelector('#sMsg').textContent=r.ok?'Test roast order created.':(x.error||'Could not create order')}}
function installTop(){const sales=topButton('pcSalesBtn','Sales');if(sales)sales.onclick=salesView;const cu=topButton('pcCashupBtn','Cash up');if(cu)cu.onclick=cashup;const mo=topButton('pcMoneyBtn','Money Owed');if(mo)mo.onclick=moneyOwed;const sr=topButton('pcSundayBtn','Sunday Roast');if(sr)sr.onclick=sunday}
function forceTestRoutes(){try{if(typeof TABLE_API!=='undefined'){} }catch{}const q=$x('qrCodesBtn');if(q)q.onclick=qrCodes}
window.addEventListener('load',()=>{installPay();installTop();forceTestRoutes();const tag=document.querySelector('.tag');if(tag)tag.textContent='PC TEST · TABLE SERVICE';const st=$x('status');if(st)st.textContent='PC TEST · safe table/KDS/payment data';});
})();
