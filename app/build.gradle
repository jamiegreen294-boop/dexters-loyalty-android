plugins { id 'com.android.application' }

android {
    namespace 'co.dexters.terminal'
    compileSdk 35

    defaultConfig {
        applicationId 'co.dexters.terminal'
        minSdk 26
        targetSdk 35
        versionCode 1
        versionName '1.0-foodhub'
    }

    buildFeatures {
        aidl = true
    }
}
