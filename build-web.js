const fs=require('fs');const zlib=require('zlib');const path=require('path');const parts=[1,2,3,4].map(n=>fs.readFileSync(path.join(__dirname,'web',`app-payload-${n}.txt`),'utf8').trim()).join('');const wrapper=zlib.gunzipSync(Buffer.from(parts,'base64')).toString('utf8');if(!wrapper.includes('Customer Offers')||!wrapper.includes('Menu Manager')||!wrapper.includes('Admin & Staff Manager'))throw new Error('Integrated app validation failed');fs.rmSync(path.join(__dirname,'dist'),{recursive:true,force:true});fs.mkdirSync(path.join(__dirname,'dist'),{recursive:true});fs.writeFileSync(path.join(__dirname,'dist','index.html'),wrapper);console.log('Built integrated Dexter\'s Loyalty app',wrapper.length);