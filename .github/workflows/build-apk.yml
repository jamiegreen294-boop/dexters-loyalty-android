name: Build Dexter's Foodhub Terminal APK

on:
  push:
    branches: [ foodhub-terminal-native-v1 ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle 8.9
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Build Dexter's terminal debug APK
        run: gradle --no-daemon assembleDebug

      - name: Rename APK
        run: cp app/build/outputs/apk/debug/app-debug.apk Dexters-Foodhub-Terminal.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Dexters-Foodhub-Terminal-APK
          path: Dexters-Foodhub-Terminal.apk
          if-no-files-found: error
